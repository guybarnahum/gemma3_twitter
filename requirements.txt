# Core
torch>=2.2
transformers>=4.41.0
datasets>=2.19.0
accelerate>=0.30.0
peft>=0.11.0
trl>=0.9.0

# Tokenization / serialization
sentencepiece>=0.1.99
safetensors>=0.4.3
protobuf>=4.25.0
einops>=0.7.0
pyarrow>=15.0.0

# Optional: 4-bit quant (Linux-only)
bitsandbytes>=0.43.0 ; platform_system != "Darwin"

# Quality of life
tqdm>=4.66.0

